apiVersion: v1
kind: PersistentVolume
metadata:
    name: pv-1
spec:
    capacity:
        storage: 10Gi
    accessModes:
        - ReadWriteOnce
    gcePersistentDisk:
        #Este eh o nome do Disco criado no GCP
        pdName: pv-disk
    storageClassName: standard